@model IEnumerable<GTD.Models.Context>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContextName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContextName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContextId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContextId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContextId })
        </td>
    </tr>
}

</table>
